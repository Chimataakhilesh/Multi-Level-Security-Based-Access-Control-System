# Multi-Level-Security-Based-Access-Control-System
This project implements a secure access control system using password and OTP verification. Like 8-digit password via keypad, verified with EEPROM data. And 4-digit OTP is sent via GSM and must be entered for access. A DC motor simulates locker opening, and alerts are sent on failure.
